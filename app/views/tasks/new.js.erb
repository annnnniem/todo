$( document ).ready(function() {
    console.log( "ready!" );

m = $('#modal');
m.html('<%= j(render partial: 'task_form', locals: {task: @task}) %>');
$('#cancel').click(function(){
	m.html('');
})
});